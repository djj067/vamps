[project]
name = "transcribe-demo"
version = "0.1.0"
description = "Meeting recorder -> live transcription -> functional spec (demo)"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "python-multipart>=0.0.9",
    "python-dotenv>=1.0",
    "httpx>=0.27",
    "openai>=1.30",
    "python-docx>=1.1",
]
